{
  "name": "pr-review-whatsapp-relay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Recebe reviews de PR via webhook do GitHub e envia notificações privadas via WhatsApp Cloud API.",
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "dev": "node --watch src/server.mjs",
    "start": "node src/server.mjs",
    "test": "node --test",
    "check": "node --check src/server.mjs && node --check src/config.mjs && node --check src/github-review-notification.mjs && node --check src/whatsapp-cloud-client.mjs"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
